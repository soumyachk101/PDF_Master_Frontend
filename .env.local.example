# Copy this file to .env.local and fill in the values for your local environment.
# .env.local is gitignored and should never be committed.

# URL of the backend API server
# For local development, set this to the backend server URL.
# On Vercel deployments, leave unset so requests are proxied via vercel.json rewrites.
VITE_API_URL=http://localhost:4000
